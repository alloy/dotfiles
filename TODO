* Make shell show update warning if the local dotconfig clone is behind the remote (need to update).
* Make shell warn if the local dotconfig clone is aheaad of the remote (need to push).
* Add rbenv/ruby-build?
